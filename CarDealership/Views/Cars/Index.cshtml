@using Dealership.Models;

<h1>Search our car's by price</h1>
@if (Model.Count == 0)
{
  <p>We have no car's for sale.</p>
}
<ul>
  @foreach (Item item in Model)
  {
    <li>@item.Description</li>
  
  }

</ul>
<a href="/items/new">Add a new item.</a>